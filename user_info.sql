create database payments;
use payments;
create table user_info(phone_number varchar(100),email  varchar(100),first_name  varchar(100),last_name  varchar(100),
Date_of_birth  varchar(100),user_password  varchar(100));
select * from user_info;